# docker compose 실행용 오버라이드
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:collabus}?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
